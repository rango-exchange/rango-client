import { styled } from '../../theme';

export const SwapContainer = styled('div', {
  borderRadius: '$10',
  maxWidth: '512px',
  height: '592px',
  position: 'relative',
  boxShadow: '$s',
  overflow: 'hidden',
});
