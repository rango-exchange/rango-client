import { styled } from '@stitches/react';
import React from 'react';
import { AngleLeft } from '../Icon';

const BackIcon = styled(AngleLeft, {
  position: 'absolute',
  left: '$0',
});

export interface PropTypes {}

function Settings(props: PropTypes) {
  return <></>;
}

export default Settings;
