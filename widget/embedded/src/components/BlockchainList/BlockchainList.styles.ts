import { styled } from '@rango-dev/ui';

export const ChainList = styled('ul', {
  padding: 0,
  margin: 0,
  listStyle: 'none',
  height: 325,
  overflowY: 'auto',
});

export const Content = styled('div', {
  minHeight: 325,
});
