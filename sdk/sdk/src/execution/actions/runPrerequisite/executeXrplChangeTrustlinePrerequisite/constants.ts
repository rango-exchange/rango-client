/**
 * The limit an XRPL trust line is opened with, and the least limit an existing
 * line needs for the prerequisite to be skipped.
 */
export const XRPL_TRUST_LINE_INFINITE_VALUE = '10000000000000';

/** Where a pending XRPL trust line transaction is looked up. */
export const XRPL_PUBLIC_SERVER = 'wss://xrplcluster.com/';
